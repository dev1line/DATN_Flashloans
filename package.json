{
  "name": "FlashLoan",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "truffle develop && truffle compile && truffle migrate --reset && cd client && npm run start",
    "testnet": "truffle console --network testnet && compile && migrate --reset"
  },
  "author": "NguyenTuanQuangSang",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.2",
    "@truffle/hdwallet-provider": "^1.4.1",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "truffle-solc-select": "^0.1.6"
  }
}
